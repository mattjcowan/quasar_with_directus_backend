# =============================================================================
# Docker Compose Environment Configuration
# Copy this file to .env and customize for your environment
# =============================================================================

# =============================================================================
# General Settings
# =============================================================================
NODE_ENV=development

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_USER=directus
POSTGRES_PASSWORD=directus
POSTGRES_DB=directus

# =============================================================================
# Directus Configuration
# =============================================================================
DIRECTUS_SECRET=change-this-to-a-secure-random-value
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=change-me-please
DIRECTUS_ADMIN_TOKEN=change-this-to-a-secure-random-value

# Public URL (how Directus is accessed externally)
DIRECTUS_PUBLIC_URL=http://localhost:30080/studio/

# =============================================================================
# Session & Cookie Configuration
# =============================================================================
# Session cookie name (must match between Directus and website for SSO)
SESSION_COOKIE_NAME=web_session

# Session duration (Directus format: 7d, 24h, etc.)
SESSION_COOKIE_TTL=7d

# Cookie security settings
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAME_SITE=Lax

# =============================================================================
# Internal URLs (for container-to-container communication)
# =============================================================================
# When communicating with Directus from other containers, use this URL (not the PUBLIC_URL)
# The prefix added to the URL path is unnecessary here, as we're bypassing nginx.
DIRECTUS_URL=http://directus:8055
SSR_API_URL=http://localhost:3000
